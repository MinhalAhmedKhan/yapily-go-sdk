// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package yapily

// Generated from OpenAPI doc version 12.10.2 and generator version 2.911.3

import (
	"context"
	"fmt"
	"github.com/MinhalAhmedKhan/yapily-go-sdk/internal/config"
	"github.com/MinhalAhmedKhan/yapily-go-sdk/internal/hooks"
	"github.com/MinhalAhmedKhan/yapily-go-sdk/internal/utils"
	"github.com/MinhalAhmedKhan/yapily-go-sdk/models/components"
	"github.com/MinhalAhmedKhan/yapily-go-sdk/retry"
	"net/http"
	"time"
)

// ServerList contains the list of servers available to the SDK
var ServerList = []string{
	"https://api.yapily.com",
}

// HTTPClient provides an interface for supplying the SDK with a custom HTTP client
type HTTPClient interface {
	Do(req *http.Request) (*http.Response, error)
}

// String provides a helper function to return a pointer to a string
func String(s string) *string { return &s }

// Bool provides a helper function to return a pointer to a bool
func Bool(b bool) *bool { return &b }

// Int provides a helper function to return a pointer to an int
func Int(i int) *int { return &i }

// Int64 provides a helper function to return a pointer to an int64
func Int64(i int64) *int64 { return &i }

// Float32 provides a helper function to return a pointer to a float32
func Float32(f float32) *float32 { return &f }

// Float64 provides a helper function to return a pointer to a float64
func Float64(f float64) *float64 { return &f }

// Pointer provides a helper function to return a pointer to a type
func Pointer[T any](v T) *T { return &v }

// Yapily API: The Yapily API enables connections between your application and users' banks. For more information check out our [documentation](/introduction).
//
// In particular, make sure to view our [Getting Started](/getting-started/get-started) steps if this is your first time here.
//
// While testing the API, our list of [sandbox credentials](/resources/sandbox/sandbox-credentials) maybe useful.
type Yapily struct {
	SDKVersion string
	// Before calling [Financial Data](#yapily-api-financial-data) or [Payments](#yapily-api-payments) endpoints, a consent from an end-user must be obtained.
	//
	// Consents are valid for up to 90 days for Financial Data endpoints and have a single-use for Payment endpoints i.e. a new consent must be obtained for each payment.
	//
	// NOTE: A user consent is also referred to as an 'Authorisation'.
	Authorisations *Authorisations
	// In order to access a user's Financial Data, you are required to request an [Authorisation](#tag/Authorisations) from the user to share the account information the bank has. Once a `consent-token` is obtained, you can call the necessary Financial Data endpoint(s) to retrieve the user's data.
	FinancialData *FinancialData
	// In order to make a Payment on behalf of a user, you are required to request an [Authorisation](#tag/Authorisations) from the user to authorise the user's account to make the payment from. Once a `consent-token` is obtained, you can call the necessary Payments endpoint(s) to execute a payment.
	Payments *Payments
	// The Consents endpoints are used to manage each `Consent` created by Yapily in response to an authorisation created for a user.
	//
	// The `Consent` object contains data that identifies a user's consent for a specific `Institution` within a Yapily application. Other than the id of the consent, the `institution-id` for the corresponding `Institution` and the user identifiers (`user-uuid` and `application-user-id`), it contains various details that indicates how the `Consent` can be used.
	Consents *Consents
	// An `Institution` object represents any Account Serving Payment Servicing Provider (ASPSP) that has been integrated and is accessible through the Yapily APIs (ASPSPs are entities that publish Read/Write APIs to permit, with customer consent, payments initiated by third party providers and/or make their customers financial data available to third party providers via their API endpoints).
	//
	// Any one of the following would be represented as Institution:
	//
	// - Traditional banks e.g. Santander
	// - Neo-banks e.g. Monzo
	// - Building societies e.g. Cumberland Building Society
	Institutions *Institutions
	// The Users endpoints are used to manage each user (otherwise known as the PSU) in Yapily. Each user belongs to an Application and as a consequence, so do each `Consent` created for a particular `User`.
	Users *Users
	// The Notifications endpoints provide an interactive way for user to receive notifications according to different event-types. This feature is currently in private beta. Please reach out if you require access.
	//
	//
	Notifications *Notifications
	// Variable Recurring Payments enables transfer of money between accounts held by the same person or transfer of money for business payments.
	//
	// In order to make Sweeping Variable Recurring Payments on behalf of a user, you are required to request an [Consent](#tag/Authorisations) from the user by calling the Sweeping Consent endpoint to authorise the user's account to make the payment. Once a `consent-token` is obtained, you can call the Payments endpoint to execute the Sweeping Variable Recurring Payments transaction. Before executing the payment, you have the option to confirm availability of funds in the user's account by calling the Funds Confirmation endpoint.
	//
	// See [VRP Payments](/payments/vrps/additional-information) for more information.
	VariableRecurringPayments *VariableRecurringPayments
	// Data Plus endpoints enable our customers to enrich transaction data.
	DataPlus *DataPlus
	// Hosted Consent Pages endpoints for data products
	HostedConsentPages *HostedConsentPages
	// Hosted Payment Pages endpoints for payments products
	HostedPaymentPages *HostedPaymentPages
	// Hosted Variable Recurring Payment Pages endpoints for payments products
	HostedVRPPages *HostedVRPPages
	// The constraints endpoints can be used to retrieve institution specific data requirements and rules that will apply when performing other operations.
	Constraints *Constraints
	// Application Management endpoints help with creating and managing client sub-applications.
	ApplicationManagement *ApplicationManagement
	// Webhook endpoints
	Webhooks *Webhooks
	// Application Beneficiaries Endpoints
	ApplicationBeneficiaries *ApplicationBeneficiaries
	// User Beneficiaries Endpoints
	UserBeneficiaries *UserBeneficiaries

	sdkConfiguration config.SDKConfiguration
	hooks            *hooks.Hooks
}

type SDKOption func(*Yapily)

// WithServerURL allows providing an alternative server URL
func WithServerURL(serverURL string) SDKOption {
	return func(sdk *Yapily) {
		sdk.sdkConfiguration.ServerURL = serverURL
	}
}

// WithTemplatedServerURL allows the overriding of the default server URL with a templated URL populated with the provided parameters
func WithTemplatedServerURL(serverURL string, params map[string]string) SDKOption {
	return func(sdk *Yapily) {
		if params != nil {
			serverURL = utils.ReplaceParameters(serverURL, params)
		}

		sdk.sdkConfiguration.ServerURL = serverURL
	}
}

// WithServerIndex allows the overriding of the default server by index
func WithServerIndex(serverIndex int) SDKOption {
	return func(sdk *Yapily) {
		if serverIndex < 0 || serverIndex >= len(ServerList) {
			panic(fmt.Errorf("server index %d out of range", serverIndex))
		}

		sdk.sdkConfiguration.ServerIndex = serverIndex
	}
}

// WithClient allows the overriding of the default HTTP client used by the SDK
func WithClient(client HTTPClient) SDKOption {
	return func(sdk *Yapily) {
		sdk.sdkConfiguration.Client = client
	}
}

// WithSecurity configures the SDK to use the provided security details
func WithSecurity(security components.Security) SDKOption {
	return func(sdk *Yapily) {
		sdk.sdkConfiguration.Security = utils.AsSecuritySource(security)
	}
}

// WithSecuritySource configures the SDK to invoke the Security Source function on each method call to determine authentication
func WithSecuritySource(security func(context.Context) (components.Security, error)) SDKOption {
	return func(sdk *Yapily) {
		sdk.sdkConfiguration.Security = func(ctx context.Context) (interface{}, error) {
			return security(ctx)
		}
	}
}

func WithRetryConfig(retryConfig retry.Config) SDKOption {
	return func(sdk *Yapily) {
		sdk.sdkConfiguration.RetryConfig = &retryConfig
	}
}

// WithTimeout Optional request timeout applied to each operation
func WithTimeout(timeout time.Duration) SDKOption {
	return func(sdk *Yapily) {
		sdk.sdkConfiguration.Timeout = &timeout
	}
}

// New creates a new instance of the SDK with the provided options
func New(opts ...SDKOption) *Yapily {
	sdk := &Yapily{
		SDKVersion: "0.0.1",
		sdkConfiguration: config.SDKConfiguration{
			UserAgent:  "speakeasy-sdk/go 0.0.1 2.911.3 12.10.2 github.com/MinhalAhmedKhan/yapily-go-sdk",
			ServerList: ServerList,
		},
		hooks: hooks.New(),
	}
	for _, opt := range opts {
		opt(sdk)
	}

	if sdk.sdkConfiguration.Security == nil {
		var envVarSecurity components.Security
		if utils.PopulateSecurityFromEnv(&envVarSecurity) {
			sdk.sdkConfiguration.Security = utils.AsSecuritySource(envVarSecurity)
		}
	}

	// Use WithClient to override the default client if you would like to customize the timeout
	if sdk.sdkConfiguration.Client == nil {
		sdk.sdkConfiguration.Client = &http.Client{Timeout: 60 * time.Second}
	}

	sdk.sdkConfiguration = sdk.hooks.SDKInit(sdk.sdkConfiguration)

	sdk.Authorisations = newAuthorisations(sdk, sdk.sdkConfiguration, sdk.hooks)
	sdk.FinancialData = newFinancialData(sdk, sdk.sdkConfiguration, sdk.hooks)
	sdk.Payments = newPayments(sdk, sdk.sdkConfiguration, sdk.hooks)
	sdk.Consents = newConsents(sdk, sdk.sdkConfiguration, sdk.hooks)
	sdk.Institutions = newInstitutions(sdk, sdk.sdkConfiguration, sdk.hooks)
	sdk.Users = newUsers(sdk, sdk.sdkConfiguration, sdk.hooks)
	sdk.Notifications = newNotifications(sdk, sdk.sdkConfiguration, sdk.hooks)
	sdk.VariableRecurringPayments = newVariableRecurringPayments(sdk, sdk.sdkConfiguration, sdk.hooks)
	sdk.DataPlus = newDataPlus(sdk, sdk.sdkConfiguration, sdk.hooks)
	sdk.HostedConsentPages = newHostedConsentPages(sdk, sdk.sdkConfiguration, sdk.hooks)
	sdk.HostedPaymentPages = newHostedPaymentPages(sdk, sdk.sdkConfiguration, sdk.hooks)
	sdk.HostedVRPPages = newHostedVRPPages(sdk, sdk.sdkConfiguration, sdk.hooks)
	sdk.Constraints = newConstraints(sdk, sdk.sdkConfiguration, sdk.hooks)
	sdk.ApplicationManagement = newApplicationManagement(sdk, sdk.sdkConfiguration, sdk.hooks)
	sdk.Webhooks = newWebhooks(sdk, sdk.sdkConfiguration, sdk.hooks)
	sdk.ApplicationBeneficiaries = newApplicationBeneficiaries(sdk, sdk.sdkConfiguration, sdk.hooks)
	sdk.UserBeneficiaries = newUserBeneficiaries(sdk, sdk.sdkConfiguration, sdk.hooks)

	return sdk
}
